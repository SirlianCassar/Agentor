/**
 * @license Copyright (c) Microsoft Corporation. All rights reserved.
*/
/** @internal */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var Microsoft;
(function (Microsoft) {
    var CIFramework;
    (function (CIFramework) {
        var Internal;
        (function (Internal) {
            const emptyString = "";
            class OpenPresenceDialogControl {
                // Empty Constructor
                constructor() {
                    var _a, _b, _c, _d;
                    this.isPresenceCommandFirstTimeLoad = false;
                    this.isDifferentIframeLoad = false;
                    this.isSetPresenceFromCacheFCSEnabled = false;
                    this.isReadFromLocalStorageDisabled = false;
                    (_a = Xrm.Reporting) === null || _a === void 0 ? void 0 : _a.reportSuccess(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, [{
                            name: "Constructor",
                            value: "Started"
                        }]);
                    this.isPresenceCommandFirstTimeLoad = true;
                    try {
                        if (!((_c = (_b = window.top) === null || _b === void 0 ? void 0 : _b.Microsoft) === null || _c === void 0 ? void 0 : _c.CIFramework)) {
                            this.isDifferentIframeLoad = true;
                            return;
                        }
                        else if (!window.top.Microsoft.CIFramework.PresenceCommand) {
                            window.top.Microsoft.CIFramework.PresenceCommand = {};
                            window.top.Microsoft.CIFramework.PresenceCommand.presenceCommandDeferred = {
                                promise: null,
                                resolve: null,
                                reject: null
                            };
                            window.top.Microsoft.CIFramework.PresenceCommand.presenceCommandDeferred.promise = new Promise((resolve, reject) => {
                                window.top.Microsoft.CIFramework.PresenceCommand.presenceCommandDeferred.resolve = resolve;
                                window.top.Microsoft.CIFramework.PresenceCommand.presenceCommandDeferred.reject = reject;
                            });
                            window.top.Microsoft.CIFramework.PresenceCommand.presenceCommandDeferred.promise = this.makeQuerablePromise(window.top.Microsoft.CIFramework.PresenceCommand.presenceCommandDeferred.promise);
                        }
                        this.processPresenceCommand();
                        this.isSetPresenceFromCacheFCSEnabled = Microsoft.CIFramework.Common.FCSStore.Instance.get(Microsoft.CIFramework.FCS.CIFnamespace, Microsoft.CIFramework.FCS.EnableOpenPresenceDialogSetPresence);
                        this.isReadFromLocalStorageDisabled = Microsoft.CIFramework.Common.FCSStore.Instance.get(Microsoft.CIFramework.FCS.CIFnamespace, Microsoft.CIFramework.FCS.DisableReadFromLocalStorage);
                    }
                    catch (error) {
                        // If DOMException, loading in different iframe
                        if (error instanceof DOMException) {
                            (_d = Xrm.Reporting) === null || _d === void 0 ? void 0 : _d.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error("DOMException occurred while loading presence command in different iframe:" + error.message));
                            this.isDifferentIframeLoad = true;
                        }
                    }
                }
                static get Instance() {
                    if (this.instance == null) {
                        this.instance = new OpenPresenceDialogControl();
                    }
                    return this.instance;
                }
                openPresenceDialog(e) {
                    var _a, _b, _c, _d, _e;
                    (_a = Xrm.Reporting) === null || _a === void 0 ? void 0 : _a.reportSuccess(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, [{
                            name: "OpenPresenceDialog",
                            value: "Started"
                        }]);
                    try {
                        if (this.isReadFromLocalStorageDisabled) {
                            const currentPresenceInfo = window.top.Microsoft.CIFramework.Internal.currentPresenceInfo;
                            if (!currentPresenceInfo || Object.keys(currentPresenceInfo).length === 0) {
                                (_b = Xrm.Reporting) === null || _b === void 0 ? void 0 : _b.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error(`Failed to open presence dialogue: runtime variable currentPresenceInfo is ${JSON.stringify(currentPresenceInfo)}. IsReadFromLocalStorageDisabled is true.`));
                                return;
                            }
                            window.localStorage[OpenPresenceDialogControl.CURRENT_PRESENCE_INFO] = JSON.stringify(currentPresenceInfo);
                        }
                        else {
                            // Task 4321340 - Clean up the existing flow (else block) once all the regions start using the new code flow.
                            if (!window.localStorage[OpenPresenceDialogControl.CURRENT_PRESENCE_INFO]) {
                                (_c = Xrm.Reporting) === null || _c === void 0 ? void 0 : _c.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error(`${OpenPresenceDialogControl.CURRENT_PRESENCE_INFO} is missing in local storage.`));
                                const currentPresenceInfo = window.top.Microsoft.CIFramework.Internal.currentPresenceInfo;
                                if (!currentPresenceInfo || Object.keys(currentPresenceInfo).length === 0) {
                                    (_d = Xrm.Reporting) === null || _d === void 0 ? void 0 : _d.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error(`Failed to open presence dialogue: runtime variable currentPresenceInfo is ${JSON.stringify(currentPresenceInfo)}.`));
                                    return;
                                }
                                window.localStorage[OpenPresenceDialogControl.CURRENT_PRESENCE_INFO] = JSON.stringify(currentPresenceInfo);
                            }
                        }
                        const dialogParams = {};
                        dialogParams[OpenPresenceDialogControl.LAST_BUTTON_CLICKED] = "";
                        const dialogOptions = { width: 300, height: 300, position: 1 /* XrmClientApi.Constants.WindowPosition.center */ };
                        Xrm.Navigation.openDialog(OpenPresenceDialogControl.SET_PRESENCE_MDD, dialogOptions, dialogParams).then((res) => {
                            var _a;
                            (_a = Xrm.Reporting) === null || _a === void 0 ? void 0 : _a.reportSuccess(`Presence dialogue opened.`);
                        }, (error) => {
                            var _a;
                            (_a = Xrm.Reporting) === null || _a === void 0 ? void 0 : _a.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error(`UCI error in opening presence dialogue. Error: ${JSON.stringify(error)}`));
                        });
                    }
                    catch (error) {
                        (_e = Xrm.Reporting) === null || _e === void 0 ? void 0 : _e.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, error);
                    }
                }
                addPresenceCommand(e) {
                    var _a, _b, _c, _d, _e, _f, _g, _h;
                    (_a = Xrm.Reporting) === null || _a === void 0 ? void 0 : _a.reportSuccess(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, [{
                            name: "AddPresenceCommand",
                            value: "Started"
                        }]);
                    if (this.isDifferentIframeLoad) {
                        (_b = Xrm.Reporting) === null || _b === void 0 ? void 0 : _b.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error("Presence Command not supported in this iframe"));
                        return Promise.resolve(false);
                    }
                    if (this.isPresenceCommandFirstTimeLoad || !this.isSetPresenceFromCacheFCSEnabled) {
                        this.initialize();
                    }
                    else {
                        this.handleAdditionalRefresh();
                    }
                    const deferredCommandForRibbon = (_f = (_e = (_d = (_c = window.top) === null || _c === void 0 ? void 0 : _c.Microsoft) === null || _d === void 0 ? void 0 : _d.CIFramework) === null || _e === void 0 ? void 0 : _e.PresenceCommand) === null || _f === void 0 ? void 0 : _f.presenceCommandDeferred;
                    if (deferredCommandForRibbon) {
                        (_g = Xrm.Reporting) === null || _g === void 0 ? void 0 : _g.reportSuccess(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, [{
                                name: "IsPresenceCommandDeferredAvailable",
                                value: true
                            }]);
                        return deferredCommandForRibbon.promise;
                    }
                    else {
                        (_h = Xrm.Reporting) === null || _h === void 0 ? void 0 : _h.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error("Presence Command Deferred is not available and the promise is resolved to false"));
                        return Promise.resolve(false);
                    }
                }
                processPresenceCommand() {
                    var _a, _b;
                    if (this.isReadFromLocalStorageDisabled) {
                        try {
                            if (this.isMultiSessionApp()) {
                                const globalContext = Xrm.Utility.getGlobalContext();
                                const orgId = globalContext.organizationSettings.organizationId;
                                OpenPresenceDialogControl.currentOrgCecCompatibility = orgId + OpenPresenceDialogControl.CEC_COMPATIBILITY;
                                const isOCorUREnabled = (globalContext).getCurrentAppSetting(OpenPresenceDialogControl.OC_UR_ENABLED_APP_SETTING);
                                const IsResolvedLegacyCommand = isOCorUREnabled;
                                window.localStorage[OpenPresenceDialogControl.currentOrgCecCompatibility] = IsResolvedLegacyCommand;
                                if (!IsResolvedLegacyCommand) {
                                    this.resolvePresenceCommandLegacy();
                                }
                            }
                            else {
                                (_a = Xrm.Reporting) === null || _a === void 0 ? void 0 : _a.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error("Legacy presence command is true and invoking the resolvePresenceCommandLegacy method. isReadFromLocalStorageDisabled is true"));
                                this.resolvePresenceCommandLegacy();
                            }
                        }
                        catch (error) {
                            (_b = Xrm.Reporting) === null || _b === void 0 ? void 0 : _b.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error(`Error in processPresenceCommand method: ${error.message}. isReadFromLocalStorageDisabled is true`));
                        }
                    }
                    else {
                        // Task 4321340 - Clean up the existing flow (else block) once all the regions start using the new code flow.
                        this.shouldInvokeCompatibilityCheckAction().then((result) => {
                            var _a;
                            if (result === true && !window.localStorage[OpenPresenceDialogControl.currentOrgCecCompatibility]) {
                                const globalContext = Xrm.Utility.getGlobalContext();
                                const isOCorUREnabled = (globalContext).getCurrentAppSetting(OpenPresenceDialogControl.OC_UR_ENABLED_APP_SETTING);
                                const IsResolvedLegacyCommand = isOCorUREnabled;
                                window.localStorage[OpenPresenceDialogControl.currentOrgCecCompatibility] = IsResolvedLegacyCommand;
                                if (!IsResolvedLegacyCommand) {
                                    this.resolvePresenceCommandLegacy();
                                }
                            }
                            else if (result === false) {
                                (_a = Xrm.Reporting) === null || _a === void 0 ? void 0 : _a.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error("Legacy presence command is true and invoking the resolvePresenceCommandLegacy method"));
                                this.resolvePresenceCommandLegacy();
                            }
                        }).catch((error) => {
                            var _a;
                            (_a = Xrm.Reporting) === null || _a === void 0 ? void 0 : _a.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error(`Error in processPresenceCommand method: ${error.message}`));
                        });
                    }
                }
                makeQuerablePromise(promise) {
                    // Don't modify any promise that has been already modified.
                    if (promise.isResolved)
                        return promise;
                    // Set initial state
                    let isPending = true;
                    let isRejected = false;
                    let isFulfilled = false;
                    // Observe the promise, saving the fulfillment in a closure scope.
                    const result = promise.then((v) => {
                        isFulfilled = true;
                        isPending = false;
                        return v;
                    }, (e) => {
                        var _a;
                        (_a = Xrm.Reporting) === null || _a === void 0 ? void 0 : _a.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error("Error in makeQuerablePromise method and presence is rejected"), e);
                        isRejected = true;
                        isPending = false;
                        throw e;
                    });
                    result.isFulfilled = () => { return isFulfilled; };
                    result.isPending = () => { return isPending; };
                    result.isRejected = () => { return isRejected; };
                    return result;
                }
                resolvePresenceCommandLegacy() {
                    let appUniqueName = "";
                    const globalContext = Xrm.Utility.getGlobalContext();
                    globalContext.getCurrentAppProperties().then((response) => {
                        var _a, _b, _c, _d, _e;
                        if ((_d = (_c = (_b = (_a = window.top) === null || _a === void 0 ? void 0 : _a.Microsoft) === null || _b === void 0 ? void 0 : _b.CIFramework) === null || _c === void 0 ? void 0 : _c.PresenceCommand) === null || _d === void 0 ? void 0 : _d.presenceCommandDeferred) {
                            appUniqueName = response.uniqueName;
                            // As of now, presence control can be shown only in OC and Dfm app.
                            if (appUniqueName === "OmniChannelEngagementHub" || appUniqueName === "msdfm_DynamicsForMicrosoft") {
                                window.top.Microsoft.CIFramework.PresenceCommand.presenceCommandDeferred.resolve(true);
                            }
                            else {
                                (_e = Xrm.Reporting) === null || _e === void 0 ? void 0 : _e.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error("Presence Command not supported in this app and presence resolved to false " + appUniqueName));
                                window.top.Microsoft.CIFramework.PresenceCommand.presenceCommandDeferred.resolve(false);
                            }
                        }
                    });
                }
                shouldInvokeCompatibilityCheckAction() {
                    return new Promise((resolve, reject) => {
                        var _a;
                        try {
                            const globalContext = Xrm.Utility.getGlobalContext();
                            const orgId = globalContext.organizationSettings.organizationId;
                            OpenPresenceDialogControl.currentOrgCecCompatibility = orgId + OpenPresenceDialogControl.CEC_COMPATIBILITY;
                            resolve(this.isMultiSessionApp());
                        }
                        catch (error) {
                            (_a = Xrm.Reporting) === null || _a === void 0 ? void 0 : _a.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error(`Error in shouldInvokeCompatibilityCheckAction: ${error === null || error === void 0 ? void 0 : error.message}`));
                            resolve(false);
                        }
                    });
                }
                isMultiSessionApp() {
                    var _a, _b;
                    return ((_a = Xrm === null || Xrm === void 0 ? void 0 : Xrm.App) === null || _a === void 0 ? void 0 : _a.sessions) != null || ((_b = Xrm === null || Xrm === void 0 ? void 0 : Xrm.Internal) === null || _b === void 0 ? void 0 : _b.getNavigationType()) === OpenPresenceDialogControl.MultiSessionNavigationType;
                }
                initialize() {
                    // Give CEC 9.5 seconds to resolve the presence command based
                    // on app profile, else resolve it to false so ribbon doesn't show presence
                    setTimeout(() => {
                        this.resolvePresenceCommandNotFoundIfAvailable();
                    }, 9500);
                    this.resetPresenceInLocalStorage();
                    if (this.isSetPresenceFromCacheFCSEnabled) {
                        this.awaitAndSetPresenceCommandDefferedPromiseResult();
                    }
                }
                handleAdditionalRefresh() {
                    var _a, _b, _c, _d;
                    try {
                        (_a = Xrm.Reporting) === null || _a === void 0 ? void 0 : _a.reportSuccess(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, [{
                                name: "PresenceDialogControlRefreshed",
                                value: true
                            }]);
                        // dispatch event to try set presence from cache for additional refresh of ribbon
                        // this can be scenarios like offline -> online
                        if (!OpenPresenceDialogControl.PresenceCommandDefferedPromiseResult) {
                            // if no result is set yet or APM resolved false do not try setting presence
                            (_b = Xrm.Reporting) === null || _b === void 0 ? void 0 : _b.reportSuccess(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, [{
                                    name: "DispatchSetPresenceFromCacheEvent",
                                    value: false
                                }]);
                            return;
                        }
                        const correlationid = Internal.OpenPresenceDialog.Utility.newGuid();
                        this.dispatchSetPresenceFromCacheEvent(correlationid);
                        (_c = Xrm.Reporting) === null || _c === void 0 ? void 0 : _c.reportSuccess(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, [{
                                name: "DispatchSetPresenceFromCacheEvent",
                                value: true
                            },
                            {
                                name: "CorrelationId",
                                value: correlationid
                            }]);
                    }
                    catch (e) {
                        (_d = Xrm.Reporting) === null || _d === void 0 ? void 0 : _d.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error(`Error trying to handle additional refresh: ${e === null || e === void 0 ? void 0 : e.message}`));
                    }
                }
                dispatchSetPresenceFromCacheEvent(correlationId) {
                    Internal.OpenPresenceDialog.Utility.dispatchCustomEvent(Internal.OpenPresenceDialog.CustomEvents.SetAgentPresenceFromCache, { correlationId }, window.parent);
                }
                resolvePresenceCommandNotFoundIfAvailable() {
                    var _a, _b, _c, _d, _e, _f;
                    if (!((_e = (_d = (_c = (_b = (_a = window.top) === null || _a === void 0 ? void 0 : _a.Microsoft) === null || _b === void 0 ? void 0 : _b.CIFramework) === null || _c === void 0 ? void 0 : _c.PresenceCommand) === null || _d === void 0 ? void 0 : _d.presenceCommandDeferred) === null || _e === void 0 ? void 0 : _e.promise).isFulfilled()) {
                        (_f = Xrm.Reporting) === null || _f === void 0 ? void 0 : _f.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error("Presence command is resolved to false as the promise is not resolved in 9.5 seconds"));
                        window.top.Microsoft.CIFramework.PresenceCommand.presenceCommandDeferred.resolve(false);
                    }
                }
                resetPresenceInLocalStorage() {
                    window.localStorage[OpenPresenceDialogControl.CURRENT_PRESENCE_INFO] = emptyString;
                }
                // Sets the presence command deffered promise result. This is async, so PresenceCommandDefferedPromiseResult
                // may be undefined until promise fullfills.
                awaitAndSetPresenceCommandDefferedPromiseResult() {
                    var _a, _b, _c, _d, _e, _f;
                    return __awaiter(this, void 0, void 0, function* () {
                        try {
                            const presenceCommandDeferred = (_d = (_c = (_b = (_a = window.top) === null || _a === void 0 ? void 0 : _a.Microsoft) === null || _b === void 0 ? void 0 : _b.CIFramework) === null || _c === void 0 ? void 0 : _c.PresenceCommand) === null || _d === void 0 ? void 0 : _d.presenceCommandDeferred;
                            if (!presenceCommandDeferred) {
                                (_e = Xrm.Reporting) === null || _e === void 0 ? void 0 : _e.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error(`Presence Command deffered promise undefined when setting local command result`));
                                return;
                            }
                            const promiseResult = yield presenceCommandDeferred.promise;
                            // set result value statically to use else where easily
                            OpenPresenceDialogControl.PresenceCommandDefferedPromiseResult = promiseResult;
                            this.isPresenceCommandFirstTimeLoad = false;
                        }
                        catch (e) {
                            (_f = Xrm.Reporting) === null || _f === void 0 ? void 0 : _f.reportFailure(OpenPresenceDialogControl.PRESENCE_CONTROL_NAME, new Error(`Presence Command value could not be locally set: ${e === null || e === void 0 ? void 0 : e.message}`));
                        }
                    });
                }
            }
            OpenPresenceDialogControl.CURRENT_PRESENCE_INFO = "GlobalToolBar_CurrentPresenceInfo";
            OpenPresenceDialogControl.LAST_BUTTON_CLICKED = "param_lastButtonClicked";
            OpenPresenceDialogControl.SET_PRESENCE_MDD = "SetAgentPresenceMDD";
            OpenPresenceDialogControl.CEC_COMPATIBILITY = "_CecCompatibilityForOmniChannel";
            OpenPresenceDialogControl.currentOrgCecCompatibility = "";
            OpenPresenceDialogControl.PRESENCE_CONTROL_NAME = "OpenPresenceDialogControl";
            OpenPresenceDialogControl.OC_UR_ENABLED_APP_SETTING = "msdyn_IsCCaaSOrURProvisioned";
            OpenPresenceDialogControl.MultiSessionNavigationType = 1;
            Internal.OpenPresenceDialogControl = OpenPresenceDialogControl;
        })(Internal = CIFramework.Internal || (CIFramework.Internal = {}));
    })(CIFramework = Microsoft.CIFramework || (Microsoft.CIFramework = {}));
})(Microsoft || (Microsoft = {}));
/**
 * @license Copyright (c) Microsoft Corporation. All rights reserved.
 */
/** @internal */
var Microsoft;
(function (Microsoft) {
    var CIFramework;
    (function (CIFramework) {
        var Internal;
        (function (Internal) {
            var OpenPresenceDialog;
            (function (OpenPresenceDialog) {
                class CustomEvents {
                }
                CustomEvents.SetAgentPresenceFromCache = "SetAgentPresenceFromCache";
                OpenPresenceDialog.CustomEvents = CustomEvents;
            })(OpenPresenceDialog = Internal.OpenPresenceDialog || (Internal.OpenPresenceDialog = {}));
        })(Internal = CIFramework.Internal || (CIFramework.Internal = {}));
    })(CIFramework = Microsoft.CIFramework || (Microsoft.CIFramework = {}));
})(Microsoft || (Microsoft = {}));
/**
 * @license Copyright (c) Microsoft Corporation. All rights reserved.
 */
/** @internal */
var Microsoft;
(function (Microsoft) {
    var CIFramework;
    (function (CIFramework) {
        var Internal;
        (function (Internal) {
            var OpenPresenceDialog;
            (function (OpenPresenceDialog) {
                class Utility {
                    static dispatchCustomEvent(eventName, detail, target = window) {
                        const event = new CustomEvent(eventName, {
                            detail,
                        });
                        target.dispatchEvent(event);
                    }
                    static newGuid() {
                        return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {
                            const r = (crypto.getRandomValues(new Uint32Array(1))[0] % 16), v = c == 'x' ? r : (r & 0x3 | 0x8);
                            return v.toString(16);
                        });
                    }
                }
                OpenPresenceDialog.Utility = Utility;
            })(OpenPresenceDialog = Internal.OpenPresenceDialog || (Internal.OpenPresenceDialog = {}));
        })(Internal = CIFramework.Internal || (CIFramework.Internal = {}));
    })(CIFramework = Microsoft.CIFramework || (Microsoft.CIFramework = {}));
})(Microsoft || (Microsoft = {}));
//# sourceMappingURL=msdyn_cec_internal_openDialog_ci_library.js.map

